package exercise;

/**
 * Exercise 04
 */
public final class App {

    private App() {

    }

    /**
     * Retrieves the coordinate values of a newly created Point object.
     *
     * This method initializes a new Point object with default coordinates
     * (typically (0,0))
     * and returns a string representation of these coordinates. The format
     * of the returned
     * string includes both the x and y values.
     *
     * @return A string representation of the x and y coordinates of the Point
     *         object,
     *         formatted as @return A string in the format "Point{x=[x], y=[y]}"
     */
    public static String getPointValues() {
        return null;
    }

    /**
     * Modifies the coordinates of a given Point object.
     *
     * Sets both the x and y coordinates of the provided Point object
     * to 5. The changes are made directly to the object passed as an
     * argument.
     *
     * @param point The Point object to be modified.
     */
    public static void modifyPoint(final Point point) {
        final int testNumber = 5;

    }

    /**
     * The main method that serves as the entry point for the application.
     * <p>
     * It creates an instance of {@code App} and prints the greeting
     * message to the standard output.
     * </p>
     *
     * @param args An array of command-line arguments (not used).
     */
    public static void main(final String[] args) {

    }
}
