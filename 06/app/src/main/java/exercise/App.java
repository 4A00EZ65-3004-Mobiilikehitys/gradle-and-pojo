package exercise;

import org.joda.time.LocalDate;

/**
 * Exercise 6
 *
 * The {@code App} class represents a simple application for managing students.
 * It demonstrates the usage of the {@link StudentManager} and {@link Student}
 * classes
 * by adding students to the manager and retrieving them by their ID.
 * <p>
 * The class is designed as a utility class with a private constructor to
 * prevent instantiation.
 * It contains the main method which serves as the entry point for the
 * application.
 * </p>
 */
public final class App {

    private App() {
        // Utility class, no instantiation required
    }

    /**
     * The main method that serves as the entry point for the application.
     * <p>
     * This method demonstrates the creation of a {@link StudentManager}
     * instance,
     * adding students to it, and retrieving a student by their ID. It prints
     * the details
     * of a found student or a relevant message if the student is not found.
     * </p>
     *
     * @param args Command-line arguments (not used).
     */
    public static void main(final String[] args) {

    }
}
